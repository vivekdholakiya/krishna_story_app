name: krishna_stories_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.7+8

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  flutter_tts: ^4.2.5  # Latest stable version as of 2026
  flutter_staggered_grid_view:
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  share_plus: ^10.0.2
  google_mobile_ads:
  connectivity_plus: 6.1.5
  internet_connection_checker_plus:
  package_info_plus:
  firebase_core: ^4.5.0
  firebase_analytics: ^12.1.3
  clarity_flutter: ^1.9.0
  just_audio: ^0.9.40
  path_provider: ^2.1.5
  http: ^1.2.2
  flutter_local_notifications:
  timezone:
  flutter_timezone: ^5.0.2
  in_app_review: ^2.0.12
  screenshot: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/images/caregory_img/